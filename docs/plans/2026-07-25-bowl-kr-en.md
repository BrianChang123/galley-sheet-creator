# Plan: A bowl / D bowl 한글·영문 병기

SPEC: docs/specs/2026-07-25-bowl-kr-en.md

## Task 1 — index.html: bowl 입력 칸 확장
- 1st/2nd Meal fieldset의 A/D bowl을 각각 한글·English 2칸(row)으로 교체
  (`aBowlEn1`, `dBowlEn1`, `aBowlEn2`, `dBowlEn2` 추가).

## Task 2 — app.js: 모델·렌더링·인식 반영
- `els`에 En 입력 4개 등록, `getModel()`/`load()`에 `aBowlEn`/`dBowlEn` 추가.
- `buildMealLines()`: bowl 출력 시 kr 줄 + `(en)` 줄(en 스타일), en만 있으면
  라벨 뒤 en 한 줄.
- `applyExtraction()`: bowl이 문자열(구형)·객체({kr,en}) 모두 처리.
- En 입력 change 리스너 연결.
- verify: `node --check app.js`

## Task 3 — api/recognize.js: 프롬프트 shape 변경
- aBowl/dBowl을 `{ "kr": "", "en": "" }`로 요구, dishes와 같은 kr/en 규칙.
- verify: `node --check api/recognize.js`
